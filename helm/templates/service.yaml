apiVersion: v1
kind: Service
metadata:
  name: {{ include "usermanager.name" . }}
  labels:
    {{- include "usermanager.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "usermanager.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 8081
      targetPort: http
